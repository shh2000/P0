//
// Created by AERO on 2019/11/17.
//

#include "mid_var.h"

mid_var::mid_var(value_type type_, int size_, int length_, int offset_, bool is_global_)
        : type(type_), size(size_), length(length_), offset(offset_), is_global(is_global_)
{}
